# Core deps without OCR engines (pytesseract needs a system Tesseract install;
# easyocr pulls a heavy torch dependency). ocr.py lazy-imports both — everything
# else, including the full test suite, runs on these.
openai>=1.50.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-dotenv>=1.0.1
pytest>=8.2.0
